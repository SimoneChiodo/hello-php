Per avviare il codice PHP su un server locale usare nel terminale: 
`php -S localhost:8000`  
*(`-S` indica che deve lanciare il **Server**)*  

Questo perché PHP è un codice Server-Side, che quindi viene elaborato sul Server
